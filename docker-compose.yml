services:
  qwen-tts:
    image: qwen3-tts
    build:
      context: .
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - .:/app
    stdin_open: true
    tty: true
